<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Ajout Client</title>
</head>
<body class="bg-white">

<div class="flex h-screen">
    <!-- Sidebar -->
    <div class="w-64 bg-blue-500 text-gray-800 border-r border-blue-200">
        <div class="p-4 border-b border-blue-200">
            <h1 class="text-2xl font-bold text-black text-center">Gestion Dettes</h1>
        </div>
        <ul class="mt-4">
            <!-- Dashboard -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200">
                <a href="#" class="flex items-center space-x-2">
                    <span>📊</span>
                    <span>Dashboard</span>
                </a>
            </li>
            <!-- Dettes -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200 mt-2">
                <a href="#" class="flex items-center space-x-2">
                    <span>💰</span>
                    <span>Dettes</span>
                </a>
            </li>
            <!-- Clients -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200 mt-2">
                <a href="{{path('client.index')}}" class="flex items-center space-x-2">
                    <span>👥</span>
                    <span>Clients</span>
                </a>
            </li>
            <!-- Articles -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200 mt-2">
                <a href="{{path('article.index')}}" class="flex items-center space-x-2">
                    <span>🛒</span>
                    <span>Articles</span>
                </a>
            </li>
            <!-- Demandes -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200 mt-2">
                <a href="#" class="flex items-center space-x-2">
                    <span>📝</span>
                    <span>Demandes</span>
                </a>
            </li>
            <!-- Utilisateurs -->
            <li class="hover:bg-blue-200 p-3 rounded-lg transition duration-200 mt-2">
                <a href="#" class="flex items-center space-x-2">
                    <span>🔑</span>
                    <span>Utilisateurs</span>
                </a>
            </li>
        </ul>
    </div>

    <div class="flex justify-between items-center mb-4">
            <div class="flex items-center space-x-2">
                <form method="get" action="{{ path('client.searchClientByTel') }}" class="flex items-center space-x-2">
                    <!-- Search input -->
                    <input type="text" name="searchPhone" value="{{ searchPhone ?? '' }}" placeholder="Rechercher par téléphone" 
                        class="w-2/3 p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                    
                    <!-- OK button -->
                    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg shadow">
                        OK
                    </button>
                </form>
            </div>
            <!-- Add button -->
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg shadow">
                <a href="{{path('client.store')}}">Ajouter</a>
            </button>
        </div>
        <div class="overflow-x-auto rounded-lg shadow">
            <table class="min-w-full bg-white border border-gray-200">
                <thead class="bg-blue-500 border-b border-gray-200">
                    <tr>
                        <th class="py-3 px-6 text-left text-black uppercase tracking-wider">Montant</th>
                        <th class="py-3 px-6 text-left text-black uppercase tracking-wider">Montant Versé</th>
                        <th class="py-3 px-6 text-left text-black uppercase tracking-wider">Montant Restant</th>
                        <th class="py-3 px-6 text-left text-black uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-black">
                {% for dette in dettes %}
                    <tr class="hover:bg-blue-300">
                        <td class="py-4 px-6 text-gray-800">{{ dette.montant }}</td>
                        <td class="py-4 px-6 text-gray-800">{{ dette.montant_verse }}</td>
                        <td class="py-4 px-6 text-gray-800">{{ dette.montant_restant }}</td>
                        <td class="py-4 px-6 text-gray-800">
                            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg shadow mr-2">
                                Dettes
                            </button>
                            
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="py-2 px-4 text-center">Aucun client trouvé.</td>
                    </tr>
                {% endfor %}
            </tbody>
            </table>

            <div class="mt-4 mb-4 flex justify-center space-x-2">
                {% if currentPage > 1 %}
                    <a href="{{ path('client.index', {'page': 1}) }}" class="bg-red-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">Première</a>
                    <a href="{{ path('client.index', {'page': currentPage - 1}) }}" class="bg-red-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">Précédent</a>
                {% endif %}

                {% for i in 1..totalPages %}
                    <a href="{{ path('client.index', {'page': i}) }}" class="bg-blue-{{ i == currentPage ? '700' : '500' }} hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">
                        {{ i }}
                    </a>
                {% endfor %}

                {% if currentPage < totalPages %}
                    <a href="{{ path('client.index', {'page': currentPage + 1}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">Suivant</a>
                    <a href="{{ path('client.index', {'page': totalPages}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">Dernière</a>
                {% endif %}
            </div>
        </div>
    
   
</div>

<!-- Footer -->
<footer class="bg-gray-300 text-center py-4 mt-auto border-t border-gray-300">
    <p class="text-black">&copy; 2024 Mon Application. Tous droits réservés.</p>
</footer>

</body>
</html>
